plugins {
    id("strata.base-conventions")
}

description = "Strata Starter Profiles - Default world generation profiles (Elysium, Netherveil, Enderrift)"

// This module contains only TOML config resources, no compiled code.
// The resources are packaged into the jar and loaded by strata-config at runtime.
